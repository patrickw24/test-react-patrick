import { useEffect, useState } from "react"


export const TaskTable = ()=>{

    let url= import.meta.env.VITE_URL
    let token= VITE_TOKEN
    
    const [data, setData] = useState([])


    const getData = ()=>{
        
        let response = await fetch
    }

}